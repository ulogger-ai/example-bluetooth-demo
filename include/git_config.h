#ifndef GIT_CONFIG_H
#define GIT_CONFIG_H

// Autogenerate this from a script
// The GIT_HASH should normally be the short git hash from the repository. It should always be unique for each build that is created.
// "no_cache" is a special pattern used for the demo to ensure that the same git_hash can be used multiple times but it should not be used in production.
#define GIT_HASH      "no_cache"
#define GIT_VERSION   "2.0.0"


#endif
